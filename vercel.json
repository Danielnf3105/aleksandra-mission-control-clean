{
  "name": "aleksandra-mission-control",
  "version": 2,
  "builds": [
    { "src": "package.json", "use": "@vercel/static-build" }
  ],
  "buildCommand": "npm run build",
  "outputDirectory": "out",
  "routes": [
    { "src": "/(.*)", "dest": "/$1" }
  ]
}